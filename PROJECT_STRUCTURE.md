# 📁 DY Carbon 프로젝트 구조 설계안

## 전체 폴더 트리 구조

```
web_workspace/
├── app/                          # Next.js App Router
│   ├── (auth)/                   # 인증 관련 라우트 그룹
│   │   ├── login/
│   │   │   └── page.tsx         # Google OAuth 로그인 페이지
│   │   └── layout.tsx           # 인증 레이아웃 (옵션)
│   │
│   ├── (dashboard)/              # 대시보드 라우트 그룹 (보호된 라우트)
│   │   ├── my-page/
│   │   │   └── page.tsx         # 마이페이지 (내 문의 목록)
│   │   └── layout.tsx           # 대시보드 레이아웃 (인증 체크)
│   │
│   ├── products/                 # 제품 관련 페이지
│   │   ├── page.tsx             # 제품 리스트 페이지
│   │   └── [id]/
│   │       └── page.tsx         # 제품 상세 페이지
│   │
│   ├── inquiry/                  # 문의 관련 페이지
│   │   └── page.tsx             # 문의 작성 페이지 (query: product_id)
│   │
│   ├── api/                      # API 라우트 (필요시)
│   │   ├── auth/
│   │   │   └── callback/
│   │   │       └── route.ts     # OAuth 콜백 처리
│   │   ├── products/
│   │   │   └── route.ts         # 제품 API (필요시)
│   │   ├── inquiries/
│   │   │   ├── route.ts         # 문의 CRUD API
│   │   │   └── [id]/
│   │   │       └── route.ts     # 문의 개별 API
│   │   └── ai/
│   │       └── route.ts         # AI 응답 생성 API
│   │
│   ├── actions/                  # Server Actions (권장)
│   │   ├── auth.ts              # 인증 관련 액션
│   │   ├── products.ts          # 제품 관련 액션
│   │   ├── inquiries.ts         # 문의 관련 액션
│   │   └── ai.ts                # AI 관련 액션
│   │
│   ├── layout.tsx                # 루트 레이아웃
│   ├── page.tsx                  # 메인 페이지
│   ├── globals.css               # 전역 스타일
│   └── loading.tsx               # 전역 로딩 UI
│
├── components/                    # 컴포넌트
│   ├── ui/                       # 재사용 가능한 기본 UI 컴포넌트
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── input.tsx
│   │   ├── textarea.tsx
│   │   ├── badge.tsx
│   │   ├── dialog.tsx
│   │   └── ...                   # shadcn/ui 기반 컴포넌트
│   │
│   ├── domain/                   # 도메인별 비즈니스 컴포넌트
│   │   ├── auth/
│   │   │   ├── login-button.tsx
│   │   │   └── user-menu.tsx
│   │   ├── product/
│   │   │   ├── product-card.tsx
│   │   │   ├── product-detail.tsx
│   │   │   └── product-list.tsx
│   │   ├── inquiry/
│   │   │   ├── inquiry-form.tsx
│   │   │   ├── inquiry-list.tsx
│   │   │   ├── inquiry-item.tsx
│   │   │   └── ai-response.tsx
│   │   └── layout/
│   │       ├── header.tsx
│   │       ├── footer.tsx
│   │       └── nav.tsx
│   │
│   └── providers/                # Context Providers
│       ├── auth-provider.tsx
│       └── theme-provider.tsx    # (옵션)
│
├── lib/                           # 유틸리티 및 설정
│   ├── supabase/
│   │   ├── client.ts             # 클라이언트 Supabase 인스턴스
│   │   ├── server.ts             # 서버 Supabase 인스턴스
│   │   └── middleware.ts         # 미들웨어용 Supabase
│   │
│   ├── ai/
│   │   └── client.ts             # AI 클라이언트 (OpenAI/Gemini)
│   │
│   ├── utils/
│   │   ├── cn.ts                 # className 유틸리티 (clsx/tailwind-merge)
│   │   ├── format.ts             # 날짜/숫자 포맷팅
│   │   └── validation.ts         # 유효성 검사
│   │
│   └── constants/
│       ├── routes.ts             # 라우트 상수
│       └── config.ts             # 앱 설정
│
├── hooks/                         # 커스텀 훅
│   ├── use-auth.ts               # 인증 상태 관리
│   ├── use-products.ts           # 제품 데이터 훅
│   ├── use-inquiries.ts          # 문의 데이터 훅
│   ├── use-ai.ts                 # AI 응답 훅
│   └── use-debounce.ts           # 디바운스 유틸리티
│
├── types/                         # TypeScript 타입 정의
│   ├── database.ts               # Supabase DB 스키마 타입
│   ├── api.ts                    # API 요청/응답 타입
│   ├── domain.ts                 # 도메인 모델 타입
│   └── index.ts                  # 타입 재export
│
├── middleware.ts                  # Next.js 미들웨어 (인증 체크)
│
├── docs/                          # 문서
│   ├── tech-stack.md             # 기술 스택 명세서
│   └── db-schema.md              # 데이터베이스 설계 가이드
│
├── public/                        # 정적 파일
│   └── ...
│
├── .env.local                     # 환경 변수 (로컬)
├── .env.example                   # 환경 변수 예시
├── next.config.ts
├── tsconfig.json
├── tailwind.config.ts
├── package.json
└── README.md
```

---

## 📋 구조 설계 원칙

### 1. **App Router 기반 라우팅**
- `(auth)`, `(dashboard)` 라우트 그룹으로 논리적 분리
- 동적 라우트 `[id]` 활용
- Server Actions 우선, API Routes는 필요시만 사용

### 2. **컴포넌트 계층 분리**
- **`ui/`**: 재사용 가능한 기본 컴포넌트 (shadcn/ui 기반)
- **`domain/`**: 비즈니스 로직이 포함된 도메인 컴포넌트
- **`providers/`**: 전역 상태 관리 (Context API)

### 3. **데이터 접근 계층**
- **`lib/supabase/`**: Supabase 클라이언트 분리 (client/server/middleware)
- **`app/actions/`**: Server Actions로 데이터 변경 처리
- **`hooks/`**: 클라이언트 데이터 페칭 및 상태 관리

### 4. **타입 안정성**
- **`types/database.ts`**: Supabase 자동 생성 타입
- **`types/domain.ts`**: 도메인 모델 타입
- **`types/api.ts`**: API 인터페이스 타입

### 5. **확장성 고려**
- Phase 2 확장을 위한 구조적 여유
- 도메인별 폴더 구조로 기능 추가 용이
- 컴포넌트 재사용성 극대화

---

## 🔄 데이터 흐름

```
Page Component
    ↓
Server Action / API Route
    ↓
lib/supabase/server.ts
    ↓
Supabase Database
```

```
Client Component
    ↓
Custom Hook (use-*.ts)
    ↓
lib/supabase/client.ts
    ↓
Supabase Database (Realtime)
```

---

## 📦 주요 의존성 (예상)

- **UI**: shadcn/ui, Lucide React, Tailwind CSS
- **인증**: @supabase/auth-helpers-nextjs
- **데이터베이스**: @supabase/supabase-js
- **AI**: openai 또는 @google/generative-ai
- **유틸리티**: clsx, tailwind-merge, zod (검증)

---

## ✅ 다음 단계

이 구조가 적합하다면, 빈 파일들을 생성하겠습니다.
